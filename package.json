{
    "scripts": {
        "install": "curl -L -O https://github.com/gohugoio/hugo/releases/download/v0.56.3/hugo_0.56.3_Linux-64bit.tar.gz && tar -xzf hugo_0.56.3_Linux-64bit.tar.gz",
        "build": "which gcc && cd vue-components && npm ci && npm run build && cd - && ./hugo"
    },
    "engines": {
        "node": "^10"
    }
}
