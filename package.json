
{
    "scripts": {
        "install": "bash ./build-hugo.sh",
        "build": "GOPATH=$HOME/go cd vue-components && npm ci && npm run build && cd - && $GOPATH/bin/hugo"
    },
    "engines": {
        "node": "^10"
    }
}
